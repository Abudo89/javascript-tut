//It works with objects the same way it works with arrays:
const myObj = {
    name: 'Abudo',
    luckyNum: 19,
    lovesFootball: true,
  };
  
  console.log(myObj);
  console.table(myObj);
